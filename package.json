{
  "name": "herstyle",
  "version": "2.0.0",
  "private": true,
  "description": "HERSTYLE monorepo — frontend (Tailwind) + backend (Express)",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "dev": "npm run build --prefix frontend && concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
